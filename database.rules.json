{
  "rules": {
    "presence": {
      ".read": "auth != null",
      ".indexOn": ["online"],
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    ".read": false,
    ".write": false
  }
}
